pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Building Docker image'
              
            }
        }
    }
    post {
        always {
            echo 'Now What'
        }
    }
}
